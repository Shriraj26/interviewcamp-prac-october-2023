"""

Here we only use visiting instead of marking it as visited as we want to go through that node again

dfs:
    
    if target, 
        add it in main array, 
        pop
        return 

    put node in array

    loop neighbor
        call dfs on neighbor
    
    

"""